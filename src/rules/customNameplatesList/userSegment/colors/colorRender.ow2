<%
    // ============ RGB COLOR ==============
    if(user.outlineColor.includes(',')) {
        %>Custom Color("<%= user.outlineColor %>")<% 
    // ============= EXISTING INGAME COLOR ===============
    } else if(false) { 
        %>Color("<%= user.outlineColor %>")<%
    // ============= EXISTING SUBROUTINE COLOR ==============
    } else {
        %>Custom String("<%= user.outlineColor %>")<% 
    }
%>
